<%= form_for(@book) do |f| %>
  <div class="form-group">
    <%= f.label :Title %></br>
    <%= f.text_field :title %>
  </div>

  <div class="form-group">
  	<%= f.label :Opinion %></br>
    <%= f.text_area :body %>
  </div>

  <div class="form-group">
    <button class="btn btn-success">Create book</button>
  </div>
<% end %>